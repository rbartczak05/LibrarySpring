services:
  # Broker wiadomości RabbitMQ
  rabbitmq:
    image: rabbitmq:3-management
    container_name: library_rabbitmq
    ports:
      - "5672:5672"   # Port komunikacyjny AMQP
      - "15672:15672" # Panel administracyjny UI
    healthcheck:
      test: [ "CMD", "rabbitmq-diagnostics", "-q", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Instancja bazy danych MongoDB
  mongodb-user:
    image: mongo:8.3.2
    container_name: library_mongodb_user
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data1:/data/db # Wolumen do trzymania danych
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "db.adminCommand('ping')" ]
      interval: 5s
      timeout: 5s
      retries: 5

  mongodb-library:
    image: mongo:8.3.2
    container_name: library_mongodb_library
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data2:/data/db
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "db.adminCommand('ping')" ]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data1:
  mongodb_data2: